export type Plan = {
  title: string;
  icon?: string;
  subtitle?: string;
  description?: string;
  price?: string;
  popular?: boolean;
  custom?: boolean;
  buttonText: string;
  features: string[];
};